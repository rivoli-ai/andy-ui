/**
 * Variant enum for UI components
 * 
 * This is a local copy/re-export of the Variant enum from @omnifex/ui-components
 * to work around Create React App's restriction on importing outside of src/
 */
export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  INVERSE = 'inverse',
}

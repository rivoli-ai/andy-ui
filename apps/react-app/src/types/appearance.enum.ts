/**
 * Appearance enum for UI components
 * 
 * This is a local copy/re-export of the Appearance enum from @omnifex/ui-components
 * to work around Create React App's restriction on importing outside of src/
 */
export enum Appearance {
  OUTLINED = 'outlined',
  FILLED = 'filled',
}

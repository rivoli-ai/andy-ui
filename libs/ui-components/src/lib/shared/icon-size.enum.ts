/**
 * Icon button size scale (Figma: large / medium / small + flex fill)
 */
export enum OmnifexIconSize {
  /** Fill the host container (set width/height on parent or :host) */
  FLEX = 'flex',
  LARGE = 'large',
  MEDIUM = 'medium',
  SMALL = 'small',
}

/**
 * Appearance enum for UI components
 * 
 * Used to define different visual appearances of components
 */
export enum OmnifexAppearance {
  FILLED = 'filled',
  OUTLINED = 'outlined',
  /** Ghost / text button (Figma: basic) */
  BASIC = 'basic',
}

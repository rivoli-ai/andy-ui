/**
 * Variant enum for UI components
 * 
 * Used to define different visual variants of components
 */
export enum OmnifexVariant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  INVERSE = 'inverse',
}
